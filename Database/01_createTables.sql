-- 1. Thông tin Người dùng (User)
CREATE TABLE APP_USER (
    USER_ID      NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FULL_NAME    VARCHAR2(100) NOT NULL,
    PHONE_NUMBER VARCHAR2(20) UNIQUE NOT NULL,
    ADDRESS      VARCHAR2(255),
    BIRTH        DATE,
    GENDER       VARCHAR2(10) ,
    USER_TYPE    VARCHAR2(20) DEFAULT 'CUSTOMER' ,
    CREATED_AT   DATE DEFAULT SYSDATE,
    UPDATED_AT   DATE DEFAULT SYSDATE,
    IS_DELETED   NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1)),
    CONSTRAINT CHK_GENDER_SCOPE CHECK (GENDER IN ('Male', 'Female')),
    CONSTRAINT CHK_USERTYPE_SCOPE CHECK (USER_TYPE IN ('ADMIN', 'STAFF', 'CUSTOMER'))
);

-- 2. Tài khoản đăng nhập
CREATE TABLE ACCOUNT (
    ACCOUNT_ID    NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID       NUMBER(10) NOT NULL,
    USERNAME      VARCHAR2(50) UNIQUE NOT NULL,
    PASSWORD_HASH VARCHAR2(200) NOT NULL,
    STATUS        VARCHAR2(20) DEFAULT 'ACTIVE',
    CREATED_AT    DATE DEFAULT SYSDATE,
    UPDATED_AT    DATE DEFAULT SYSDATE,
    IS_DELETED    NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 3. Phân hệ chức năng
CREATE TABLE FUNCTION (
    FUNCTION_ID   NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME_FUNCTION VARCHAR2(100) NOT NULL,
    CREATED_AT    DATE DEFAULT SYSDATE,
    UPDATED_AT    DATE DEFAULT SYSDATE,
    IS_DELETED    NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 4. Quyền chi tiết
CREATE TABLE ROLE (
    ROLE_ID       NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FUNCTION_ID   NUMBER(10) NOT NULL,
    ROLE_NAME     VARCHAR2(100) NOT NULL,
    ADD_PERM      NUMBER(1) DEFAULT 0 CHECK (ADD_PERM IN (0,1)),
    EDIT_PERM     NUMBER(1) DEFAULT 0 CHECK (EDIT_PERM IN (0,1)),
    DELETE_PERM   NUMBER(1) DEFAULT 0 CHECK (DELETE_PERM IN (0,1)),
    VIEW_PERM     NUMBER(1) DEFAULT 0 CHECK (VIEW_PERM IN (0,1)),
    DOWNLOAD_PERM NUMBER(1) DEFAULT 0 CHECK (DOWNLOAD_PERM IN (0,1)),
    CREATED_AT    DATE DEFAULT SYSDATE,
    UPDATED_AT    DATE DEFAULT SYSDATE,
    IS_DELETED    NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 5. Nhóm quyền
CREATE TABLE ROLE_GROUP (
    ROLE_GROUP_ID   NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME_ROLE_GROUP VARCHAR2(100) NOT NULL,
    CREATED_AT      DATE DEFAULT SYSDATE,
    UPDATED_AT      DATE DEFAULT SYSDATE,
    IS_DELETED      NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 6. Gán Quyền vào Nhóm
CREATE TABLE ROLE_GROUP_ASSIGN_ROLE (
    ROLE_GROUP_ID NUMBER(10) NOT NULL,
    ROLE_ID       NUMBER(10) NOT NULL,
    CREATED_AT      DATE DEFAULT SYSDATE,
    UPDATED_AT      DATE DEFAULT SYSDATE,
    IS_DELETED      NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1)),
    PRIMARY KEY (ROLE_GROUP_ID, ROLE_ID)
);

-- 7. Gán Tài khoản vào Nhóm
CREATE TABLE ACCOUNT_ASSIGN_ROLE_GROUP (
    ACCOUNT_ID    NUMBER(10) NOT NULL,
    ROLE_GROUP_ID NUMBER(10) NOT NULL,
    CREATED_AT      DATE DEFAULT SYSDATE,
    UPDATED_AT      DATE DEFAULT SYSDATE,
    IS_DELETED      NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1)),
    PRIMARY KEY (ACCOUNT_ID, ROLE_GROUP_ID)
);

-- 8. Gán Quyền trực tiếp cho Tài khoản
CREATE TABLE ACCOUNT_ASSIGN_ROLE (
    ACCOUNT_ID NUMBER(10) NOT NULL,
    ROLE_ID    NUMBER(10) NOT NULL,
    CREATED_AT      DATE DEFAULT SYSDATE,
    UPDATED_AT      DATE DEFAULT SYSDATE,
    IS_DELETED      NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1)),
    PRIMARY KEY (ACCOUNT_ID, ROLE_ID)
);

-- 9. Token phiên làm việc
CREATE TABLE ACCOUNT_TOKEN (
    TOKEN_ID      NUMBER(20) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ACCOUNT_ID    NUMBER(10) NOT NULL,
    TOKEN_VALUE   VARCHAR2(500) UNIQUE NOT NULL,
    EXPIRES_AT    DATE NOT NULL,
    ISSUED_AT     DATE DEFAULT SYSDATE,
    IS_REVOKED    CHAR(1) DEFAULT 'N' CHECK (IS_REVOKED IN ('Y', 'N'))
);

-- 10. Danh mục sản phẩm
CREATE TABLE CATEGORY (
    CATEGORY_ID        NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME               VARCHAR2(40) NOT NULL,
    PARENT_CATEGORY_ID NUMBER(10),
    CREATED_AT         DATE DEFAULT SYSDATE,
    UPDATED_AT         DATE DEFAULT SYSDATE,
    IS_DELETED         NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 11. Sản phẩm
CREATE TABLE PRODUCT (
    PRODUCT_ID     NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME           VARCHAR2(100) NOT NULL,
    DESCRIPTION    VARCHAR2(255),
    PRICE          NUMBER(18,2) NOT NULL CHECK (PRICE >= 0),
    BRAND          VARCHAR2(50) NOT NULL,
    STATUS         NUMBER(1) DEFAULT 1 CHECK (STATUS IN (0,1)),
    WARRANTY_MONTH NUMBER(3) NOT NULL,
    CATEGORY_ID    NUMBER(10) NOT NULL,
    CREATED_AT     DATE DEFAULT SYSDATE,
    UPDATED_AT     DATE DEFAULT SYSDATE,
    IS_DELETED     NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 12. Đối tác Affiliate
CREATE TABLE AFFILIATE (
    AFFILIATE_ID    NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID         NUMBER(10) NOT NULL,
    COMMISSION_RATE NUMBER(5,2) DEFAULT 5.0,
    TOTAL_EARNED    NUMBER(18,2) DEFAULT 0,
    STATUS          VARCHAR2(20) DEFAULT 'ACTIVE',
    CREATED_AT      DATE DEFAULT SYSDATE,
    UPDATED_AT      DATE DEFAULT SYSDATE,
    IS_DELETED      NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 13. Mã giảm giá (Coupon)
CREATE TABLE COUPON (
    COUPON_ID        NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODE             NVARCHAR2(30) NOT NULL UNIQUE,
    DISCOUNT_TYPE    NVARCHAR2(10) NOT NULL CHECK (DISCOUNT_TYPE IN ('PERCENTAGE', 'AMOUNT')),
    DISCOUNT_VALUE   DECIMAL(18,2) NOT NULL CHECK (DISCOUNT_VALUE >= 0),
    MIN_ORDER_VALUE  DECIMAL(18,2) DEFAULT 0 CHECK (MIN_ORDER_VALUE >= 0),
    MAX_DISCOUNT     DECIMAL(18,2) CHECK (MAX_DISCOUNT >= 0),
    START_AT         DATE NOT NULL,
    END_AT           DATE NOT NULL,
    IS_ACTIVE        NUMBER(1) DEFAULT 1 CHECK (IS_ACTIVE IN (0,1)),
    DESCRIPTION      NVARCHAR2(255),
    AFFILIATE_ID     NUMBER(10),
    CREATED_AT       DATE DEFAULT SYSDATE,
    UPDATED_AT       DATE DEFAULT SYSDATE,
    IS_DELETED       NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1)),
    CONSTRAINT CHK_COUPON_DATES CHECK (END_AT > START_AT)
);

-- 14. Kho hàng
CREATE TABLE WAREHOUSE (
    WAREHOUSE_ID NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME         VARCHAR2(100) NOT NULL,
    ADDRESS      VARCHAR2(255) NOT NULL,
    CREATED_AT   DATE DEFAULT SYSDATE,
    UPDATED_AT   DATE DEFAULT SYSDATE,
    IS_DELETED   NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 15. Tồn kho chi tiết
CREATE TABLE INVENTORY (
    PRODUCT_ID   NUMBER(10) NOT NULL,
    WAREHOUSE_ID NUMBER(10) NOT NULL,
    QUANTITY     NUMBER(10) DEFAULT 0 CHECK (QUANTITY >= 0),
    CREATED_AT   DATE DEFAULT SYSDATE,
    UPDATED_AT   DATE DEFAULT SYSDATE,
    IS_DELETED   NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1)),
    PRIMARY KEY (PRODUCT_ID, WAREHOUSE_ID)
);

-- 16. Đơn hàng
CREATE TABLE ORDERS (
    ORDER_ID         NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID          NUMBER(10) NOT NULL,
    COUPON_ID        NUMBER,
    SUBTOTAL         NUMBER(18,2) DEFAULT 0 NOT NULL,
    DISCOUNT_AMOUNT  NUMBER(18,2) DEFAULT 0,
    SHIPPING_FEE     NUMBER(18,2) DEFAULT 0,
    TOTAL            NUMBER(18,2) DEFAULT 0 NOT NULL,
    STATUS           VARCHAR2(30) NOT NULL,
    RECEIVER_NAME    VARCHAR2(100) NOT NULL,
    RECEIVER_PHONE   VARCHAR2(20) NOT NULL,
    SHIPPING_ADDRESS NVARCHAR2(500) NOT NULL,
    CREATED_AT       DATE DEFAULT SYSDATE,
    UPDATED_AT       DATE DEFAULT SYSDATE,
    IS_DELETED       NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1)),
    CONSTRAINT CHK_ORDER_STATUS_SCOPE  CHECK (STATUS IN ('PENDING','CONFIRMED','PROCESSING','SHIPPING','DELIVERED','CANCEL'))
);

-- 17. Chi tiết đơn hàng
CREATE TABLE ORDER_DETAIL (
    ORDER_DETAIL_ID NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ORDER_ID        NUMBER(10) NOT NULL,
    PRODUCT_ID      NUMBER(10) NOT NULL,
    WAREHOUSE_ID    NUMBER(10),
    QUANTITY        NUMBER(10) NOT NULL CHECK (QUANTITY > 0),
    PRICE           NUMBER(18,2) NOT NULL,
    LINE_TOTAL      NUMBER(18,2) DEFAULT 0
);

-- 18. Thanh toán
CREATE TABLE PAYMENT (
    PAYMENT_ID       NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ORDER_ID         NUMBER(10) NOT NULL,
    PAYMENT_METHOD   VARCHAR2(30) NOT NULL,
    AMOUNT           NUMBER(18,2) NOT NULL,
    PAYMENT_STATUS   VARCHAR2(20) DEFAULT 'PENDING',
    PAID_AT          DATE,
    CREATED_AT       DATE DEFAULT SYSDATE,
    UPDATED_AT       DATE DEFAULT SYSDATE
);

-- 19. Chiến dịch khuyến mãi
CREATE TABLE PROMOTION (
    PROMOTION_ID   NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    PROMOTION_NAME VARCHAR2(120) NOT NULL,
    START_AT       DATE NOT NULL,
    END_AT         DATE NOT NULL,
    IS_ACTIVE      NUMBER(1) DEFAULT 1,
    CREATED_AT     DATE DEFAULT SYSDATE,
    UPDATED_AT     DATE DEFAULT SYSDATE,
    IS_DELETED     NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 20. Sản phẩm thuộc khuyến mãi
CREATE TABLE PROMOTION_PRODUCT (
    PROMOTION_ID   NUMBER(10) NOT NULL,
    PRODUCT_ID     NUMBER(10) NOT NULL,
    DISCOUNT_VALUE NUMBER(18,2),
    PRIMARY KEY (PROMOTION_ID, PRODUCT_ID)
);

-- 21. Đánh giá
CREATE TABLE REVIEW (
    REVIEW_ID   NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID     NUMBER(10) NOT NULL,
    PRODUCT_ID  NUMBER(10) NOT NULL,
    RATING      NUMBER(1) CHECK (RATING BETWEEN 1 AND 5),
    CONTENT     VARCHAR2(500),
    CREATED_AT     DATE DEFAULT SYSDATE,
    UPDATED_AT     DATE DEFAULT SYSDATE,
    IS_DELETED     NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 22. Giỏ hàng
CREATE TABLE CART (
    CART_ID    NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID    NUMBER(10) NOT NULL,
    CREATED_AT DATE DEFAULT SYSDATE,
    UPDATED_AT     DATE DEFAULT SYSDATE,
    IS_DELETED     NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))
);

-- 23. Chi tiết giỏ hàng
CREATE TABLE CART_ITEM (
    CART_ITEM_ID NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CART_ID      NUMBER(10) NOT NULL,
    PRODUCT_ID   NUMBER(10) NOT NULL,
    QUANTITY     NUMBER(10) NOT NULL CHECK (QUANTITY > 0),
    CONSTRAINT UQ_CART_PRODUCT UNIQUE (CART_ID, PRODUCT_ID)
);

-- 24. Điểm tích lũy
CREATE TABLE LOYALTY_POINT (
    USER_ID      NUMBER(10) NOT NULL PRIMARY KEY,
    TOTAL_POINTS NUMBER(10) DEFAULT 0,
    UPDATED_AT   DATE DEFAULT SYSDATE
);

-- 25. Mua chung (Group Buy)
CREATE TABLE GROUP_BUY (
    GROUP_BUY_ID   NUMBER(10) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    PRODUCT_ID     NUMBER(10) NOT NULL,
    HOST_USER_ID   NUMBER(10) NOT NULL,
    INVITE_CODE    VARCHAR2(20) UNIQUE NOT NULL,
    TARGET_MEMBER  NUMBER(10) NOT NULL,
    CURRENT_MEMBER NUMBER(10) DEFAULT 1,
    STATUS         VARCHAR2(20) DEFAULT 'PROCESSING',
    CREATED_AT DATE DEFAULT SYSDATE,
    UPDATED_AT     DATE DEFAULT SYSDATE,
    IS_DELETED     NUMBER(1) DEFAULT 0 CHECK (IS_DELETED IN (0,1))

);

-- 26. Thành viên tham gia mua chung
CREATE TABLE GROUP_BUY_PARTICIPANT (
    GROUP_BUY_ID NUMBER(10) NOT NULL,
    USER_ID      NUMBER(10) NOT NULL,
    ORDER_ID     NUMBER(10),
    PRIMARY KEY (GROUP_BUY_ID, USER_ID)
);
select * from COUPON;
-- sample
-- 1. Tạo danh mục sản phẩm
INSERT INTO CATEGORY (NAME) VALUES ('Electronics');

-- 2. Tạo sản phẩm mẫu
INSERT INTO PRODUCT (NAME, DESCRIPTION, PRICE, BRAND, WARRANTY_MONTH, CATEGORY_ID)
VALUES ('iPhone 15 Pro', '256GB, Titanium', 28000000, 'Apple', 12, 1);

-- 3. Tạo kho hàng và tồn kho
INSERT INTO WAREHOUSE (NAME, ADDRESS) VALUES ('Main Warehouse', 'Ho Chi Minh City');
INSERT INTO INVENTORY (PRODUCT_ID, WAREHOUSE_ID, QUANTITY) VALUES (1, 1, 100);

-- 4. Tạo người dùng mẫu
INSERT INTO APP_USER (FULL_NAME, PHONE_NUMBER, ADDRESS, GENDER, USER_TYPE)
VALUES ('John Doe', '0987654321', 'District 1, HCMC', 'Male', 'CUSTOMER');

-- 5. Tạo mã giảm giá (Dùng 'AMOUNT' hoặc 'PERCENTAGE' theo đúng CHECK constraint)
INSERT INTO COUPON (CODE, DISCOUNT_TYPE, DISCOUNT_VALUE, START_AT, END_AT, DESCRIPTION)
VALUES ('SALE2026', 'AMOUNT', 200000, SYSDATE - 1, SYSDATE + 30, 'Giảm 200k cho người mới');

-- 6. Tạo đơn hàng (Lưu ý ID tự tăng nên nếu là bảng mới thì ID sẽ là 1)
INSERT INTO ORDERS (
    USER_ID, COUPON_ID, SUBTOTAL, DISCOUNT_AMOUNT, SHIPPING_FEE, TOTAL,
    STATUS, RECEIVER_NAME, RECEIVER_PHONE, SHIPPING_ADDRESS
) VALUES (
    1, 1, 28000000, 200000, 30000, 27830000,
    'CONFIRMED', 'John Doe', '0987654321', 'District 1, HCMC'
);

-- 7. Tạo chi tiết đơn hàng
INSERT INTO ORDER_DETAIL (ORDER_ID, PRODUCT_ID, WAREHOUSE_ID, QUANTITY, PRICE, LINE_TOTAL)
VALUES (1, 1, 1, 1, 28000000, 28000000);

COMMIT;
